#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n;
    printf("enter the value:\n");
    scanf("%d",&n);
    if(n%2==0)
        printf("the value is even:");
        else
        printf("the value is odd:");
    return 0;
}
